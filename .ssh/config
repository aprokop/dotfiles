# Reuse existing connection
ControlMaster auto
ControlPath /home/username/.ssh/tmp/%h_%p_%r

Host proxy_name
    HostName software-login.sandia.gov
    User user1

Host remote_name
    ProxyCommand ssh -q proxy_name nc remote 22
    User user2
